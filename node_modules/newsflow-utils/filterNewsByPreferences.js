export function filterNewsByPreferences(newsList, preferences) {
  return newsList.filter(article =>
    preferences.some(pref =>
      article.title.toLowerCase().includes(pref.toLowerCase())
    )
  );
}
